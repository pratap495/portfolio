# Pratap.dev Portfolio

A responsive React portfolio recreated from the supplied visual reference.

## Run locally

```bash
npm install
npm run dev
```

Create a production bundle with `npm run build`.
